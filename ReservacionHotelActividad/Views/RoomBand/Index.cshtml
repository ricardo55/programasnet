@model IEnumerable<ReservacionHotelExamen2.Models.RoomBand>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BandDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BandDesc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoomBandID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoomBandID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoomBandID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
